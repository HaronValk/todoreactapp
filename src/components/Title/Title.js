import {StyledTitle} from "./Title.styles";

export const Title = () => {
    return(
        <header className="page-header">
            <StyledTitle>TODO REACT </StyledTitle>
        </header>
    )
}